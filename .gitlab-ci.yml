image: microsoft/dotnet:latest

stages:
  - build
  - test
  - deploy

variables:
  project: "Bubbio"

before_script:
  - "dotnet restore $project"

build:
  stage: build
  script:
    - "bash scripts/build.sh"

test:
  stage: test
  script:
    - "bash scripts/test.sh"